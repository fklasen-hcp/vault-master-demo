apiVersion: v1
kind: Service
metadata:
  name: encryption-demo-ui
  namespace: encryption-demo
  labels:
    app: encryption-demo-ui
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: encryption-demo-ui

# Made with Bob