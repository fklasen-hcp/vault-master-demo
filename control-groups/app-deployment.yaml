apiVersion: v1
kind: Namespace
metadata:
  name: controlgroups-demo
---
# ConfigMap is created separately by Makefile from app.py
# This ensures the latest version is always deployed
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: controlgroups-demo-app
  namespace: controlgroups-demo
---
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: secret-reader
  namespace: controlgroups-demo
rules:
- apiGroups: [""]
  resources: ["secrets"]
  verbs: ["get", "list"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: controlgroups-demo-secret-reader
  namespace: controlgroups-demo
subjects:
- kind: ServiceAccount
  name: controlgroups-demo-app
  namespace: controlgroups-demo
roleRef:
  kind: Role
  name: secret-reader
  apiGroup: rbac.authorization.k8s.io
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: controlgroups-demo-ui
  namespace: controlgroups-demo
spec:
  replicas: 1
  selector:
    matchLabels:
      app: controlgroups-demo-ui
  template:
    metadata:
      labels:
        app: controlgroups-demo-ui
    spec:
      serviceAccountName: controlgroups-demo-app
      containers:
      - name: app
        image: python:3.11-slim
        command: ["/bin/bash", "-c"]
        args:
          - |
            pip install flask hvac
            cd /app && python app.py
        ports:
        - containerPort: 8080
        env:
        - name: VAULT_ADDR
          value: "https://host.minikube.internal:8200"
        - name: VAULT_NAMESPACE
          value: "master-demo"
        - name: VAULT_SKIP_VERIFY
          value: "true"
        volumeMounts:
        - name: app-code
          mountPath: /app
      volumes:
      - name: app-code
        configMap:
          name: controlgroups-app

# Made with Bob
